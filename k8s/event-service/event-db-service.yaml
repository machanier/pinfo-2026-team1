apiVersion: v1
kind: Service
metadata:
  name: event-db
  namespace: unigevents
  labels:
    app: event-db
    app.kubernetes.io/part-of: unigevents
spec:
  clusterIP: None
  selector:
    app: event-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
