apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: unigevents
  labels:
    app: user-db
    app.kubernetes.io/part-of: unigevents
spec:
  clusterIP: None
  selector:
    app: user-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
