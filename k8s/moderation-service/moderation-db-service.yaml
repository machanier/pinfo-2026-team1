apiVersion: v1
kind: Service
metadata:
  name: moderation-db
  namespace: unigevents
  labels:
    app: moderation-db
    app.kubernetes.io/part-of: unigevents
spec:
  clusterIP: None
  selector:
    app: moderation-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
