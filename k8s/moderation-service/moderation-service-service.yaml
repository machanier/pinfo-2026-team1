apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: unigevents
  labels:
    app: moderation-service
    app.kubernetes.io/part-of: unigevents
    # Picked up by the shared ServiceMonitor in k8s/observability/.
    unigevents.com/scrape: quarkus
spec:
  type: ClusterIP
  selector:
    app: moderation-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
