apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: moderation-db
  namespace: unigevents
  labels:
    app: moderation-db
    app.kubernetes.io/part-of: unigevents
spec:
  serviceName: moderation-db
  replicas: 1
  selector:
    matchLabels:
      app: moderation-db
  template:
    metadata:
      labels:
        app: moderation-db
    spec:
      # PINFO-191: no API server access required for Postgres pods.
      automountServiceAccountToken: false
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: moderation_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: moderation-db-secret
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: moderation-db-secret
                  key: password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "moderation_service", "-d", "moderation_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "moderation_service", "-d", "moderation_db"]
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: microk8s-hostpath
        resources:
          requests:
            storage: 1Gi
