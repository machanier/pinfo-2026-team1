apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: unigevents
  labels:
    app: search-service
    app.kubernetes.io/part-of: unigevents
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
