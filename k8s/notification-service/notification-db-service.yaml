apiVersion: v1
kind: Service
metadata:
  name: notification-db
  namespace: unigevents
  labels:
    app: notification-db
    app.kubernetes.io/part-of: unigevents
spec:
  clusterIP: None
  selector:
    app: notification-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
