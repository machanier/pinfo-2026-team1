apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-service
  namespace: unigevents
  labels:
    app: registration-service
    app.kubernetes.io/part-of: unigevents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registration-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: registration-service
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: registration-service
          image: ghcr.io/machanier/pinfo-2026-team1/registration-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8086
          env:
            - name: DB_URL
              value: jdbc:postgresql://registration-db:5432/registrations_db
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: registration-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: registration-db-secret
                  key: password
          resources:
            requests:
              cpu: "100m"
              memory: "384Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 8086
            initialDelaySeconds: 40
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 8086
            initialDelaySeconds: 90
            periodSeconds: 15
