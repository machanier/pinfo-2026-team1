# ============================================================
# UNIGEvents — Environment Variables Template
# ============================================================
# Copy this file to .env and fill in your values:
#   cp docker/.env.example docker/.env
#
# The .env file is ignored by git (never commit it).
# ============================================================

# PostgreSQL
DB_NAME=unigEvents
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# Quarkus backend
BACKEND_PORT=8080
