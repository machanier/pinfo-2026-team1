apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: unigevents
  labels:
    app: notification-service
    app.kubernetes.io/part-of: unigevents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: notification-service
          image: ghcr.io/machanier/pinfo-2026-team1/notification-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
          env:
            - name: DB_URL
              value: jdbc:postgresql://notification-db:5432/notifications_db
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: notification-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-db-secret
                  key: password
          resources:
            requests:
              cpu: "100m"
              memory: "384Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 8083
            initialDelaySeconds: 40
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 8083
            initialDelaySeconds: 90
            periodSeconds: 15
