# === registration-service ===
quarkus.http.port=8086
quarkus.application.name=registration-service
# =====================================================
# DATABASE (shared config)
# =====================================================
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER:registration_service}
quarkus.datasource.password=${DB_PASSWORD:registration_service_pwd}
quarkus.datasource.jdbc.url=${DB_URL}
quarkus.hibernate-orm.log.sql=false
# PROD schema strategy
quarkus.hibernate-orm.database.generation=update
# =====================================================
# TEST PROFILE (IMPORTANT FIX)
# =====================================================
%test.quarkus.datasource.devservices.enabled=true
%test.quarkus.hibernate-orm.database.generation=drop-and-create
%test.quarkus.hibernate-orm.schema-management.strategy=drop-and-create
# Disable external Kafka in tests
%test.quarkus.kafka.devservices.enabled=true
# =====================================================
# REST CLIENTS
# =====================================================
quarkus.rest-client.event-service.url=http://event-service:8082
quarkus.rest-client.user-service.url=http://user-service:8081
# =====================================================
# LOGGING / OPENAPI / METRICS
# =====================================================
quarkus.log.level=INFO
quarkus.smallrye-openapi.path=/openapi
quarkus.micrometer.enabled=true
quarkus.micrometer.export.prometheus.enabled=true
quarkus.smallrye-health.root-path=/q/health
quarkus.log.category."io.smallrye.jwt".level=DEBUG
# =====================================================
# JWT
# =====================================================
mp.jwt.verify.issuer=https://dev-cy8uphtpfx5bdclo.us.auth0.com/
mp.jwt.verify.publickey.location=https://dev-cy8uphtpfx5bdclo.us.auth0.com/.well-known/jwks.json
mp.jwt.verify.audiences=https://api.unigevents.ch
mp.jwt.token.header=Authorization
# =====================================================
# KAFKA (PROD ONLY)
# =====================================================
# PINFO-199 / PINFO-201: tant qu'aucun broker Kafka n'est déployé dans le
# cluster microk8s, le readiness check par défaut de SmallRye Reactive
# Messaging (qui pingue le broker au démarrage) reste DOWN et bloque le
# rollout du Deployment. On le désactive en prod : `Emitter.send` devient
# fire-and-forget et @Incoming ne déclenche jamais en attendant qu'un
# broker soit dispo. Conséquence fonctionnelle : la cascade
# d'annulation (event.cancelled -> registrations CANCELLED) ne marche
# pas en prod tant que Kafka n'est pas déployé. À retirer une fois
# Kafka en place dans k8s/ — voir le ticket de déploiement Kafka.
quarkus.messaging.health.enabled=false
# PINFO-206: bootstrap.servers DOIT être un nom DNS résolvable au
# démarrage, sinon le client Kafka du @Incoming consumer (construit
# *eagerly* dans onApplicationStart, contrairement aux producers qui
# sont créés paresseusement) lève ConfigException("No resolvable
# bootstrap urls") et Quarkus refuse de démarrer — health.enabled=false
# ci-dessus n'aide pas car on plante AVANT les probes. On met `localhost`
# en fallback : DNS résout toujours (127.0.0.1), le consumer se construit,
# les tentatives de connexion échouent en async sans crasher l'app.
# Quand Kafka sera déployé dans k8s/, on overridera via l'env var
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092 sur le Deployment — comme on le fait
# déjà en docker-compose. Pattern aligné sur event-service.
kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
mp.messaging.outgoing.registration-confirmed.connector=smallrye-kafka
mp.messaging.outgoing.registration-confirmed.topic=registration.confirmed
mp.messaging.outgoing.registration-confirmed.value.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.outgoing.registration-waitlisted.connector=smallrye-kafka
mp.messaging.outgoing.registration-waitlisted.topic=registration.waitlisted
mp.messaging.outgoing.registration-waitlisted.value.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.outgoing.registration-cancelled.connector=smallrye-kafka
mp.messaging.outgoing.registration-cancelled.topic=registration.cancelled
mp.messaging.outgoing.registration-cancelled.value.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.incoming.event-cancelled.connector=smallrye-kafka
mp.messaging.incoming.event-cancelled.topic=event.cancelled
mp.messaging.incoming.event-cancelled.value.deserializer=org.apache.kafka.common.serialization.StringDeserializer
mp.messaging.incoming.event-cancelled.group.id=registration-service
%test.kafka.bootstrap.servers=localhost:9092
